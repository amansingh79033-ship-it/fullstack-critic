# Memory guidance

The agent's runtime memory belongs in the project being reviewed, under `.critic-memory/`. This directory documents the recommended memory model but does not contain project data.

Use the templates and rules in `docs/GETTING_STARTED.md` and `docs/ARCHITECTURE.md`. Never store credentials, tokens, private keys, customer data, or sensitive logs.
